histogram - desc
graphs - explorative

inferential
read.csv("filename.csv")
summary(re)
gives min, max, median values etc
head(re,2) # top 2 rows of data set
qunatile(re na.rm= TRUE)
gives the percentile of each value

mechn -> high level prog.
real time data

installing the liberaries, packages
html output
errors

library(shiny)
reproduceable